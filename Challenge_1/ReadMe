I found out which radio frequency is generating a structured signal, and then created a scatter plot of the data that was transmitted. Finally, I drew a line plot of the mean amplitude for each unit time on top of the scatter plot.
